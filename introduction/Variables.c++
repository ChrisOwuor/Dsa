#include <iostream>

#include <string>

using namespace std;

int main()
{
    // interger
    int age = 10;
    // using strings we use string library
    string names = "chris";
    // for decimals
    double weight = 50.6;
    // character
    char single_character = 't';
    // for floats
    float avg = 49.6f;
    // booleans
    bool isAdmin = false;

    cout << names << endl;

    return 0;
}